export interface Text {
    code: number;
    eng: string;

    shorthand?: boolean;

    normal?: boolean;
    control?: boolean;
    picture?: boolean;
    singleChar?: boolean;
    multiChar?: boolean;
    variable?: boolean;
    useTilemap?: boolean;
};
