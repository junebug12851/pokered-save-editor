.row
    mat-form-field.small
        mat-label X
        input(
            matInput=''
            type='number'
            '[(ngModel)]'='data.x'
            '[disabled]'='disabled'
        )

    mat-form-field.small
        mat-label Y
        input(
            matInput=''
            type='number'
            '[(ngModel)]'='data.y'
            '[disabled]'='disabled'
        )

    mat-form-field.small
        mat-label Text ID
        input(
            matInput=''
            type='number'
            '[(ngModel)]'='data.text'
            '[disabled]'='disabled'
        )

    button(
        mat-raised-button=''
        '(click)'='remListItem()'
        '[disabled]'='disabled'
        color="warn"
    )
        span.im.im-delete
